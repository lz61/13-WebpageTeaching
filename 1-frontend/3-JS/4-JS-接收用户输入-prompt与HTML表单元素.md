1.prompt初识

```
<head>
	<script>
		var name = prompt("请输入一个名字");
		console.log("你输入的姓名是",name);
	</script>
</head>
```

prompt解释:

```
prompt(参数)
返回值: 用户输入的内容
参数: 在跳出该输入框的时候显示的内容
```



2.HTML表单元素初识(不细讲,之后再说)

目标: 使用图形化界面接收用户输入.

