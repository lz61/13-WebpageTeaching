server:
  port: 8080

spring:
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      #[databasename]为数据库名,记得修改,例如jwxt
      # 一个项目对应一个[databasename]
      # jdbc:mysql://localhost:[mysqlRunningPort]/[databaseName]?serverTimezone=UTC
      url: jdbc:mysql://localhost:3306/test?serverTimezone=UTC
      username: root
      password: 1234